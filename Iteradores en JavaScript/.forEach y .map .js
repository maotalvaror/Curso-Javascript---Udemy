//ForEach: Se ejecutara almenos una vez que haya elementos en el arreglo
//codigo mas corto con array functions

const pendientes = ['Tarea', 'Comer', 'Proyectos', 'Estudiar JavaScript'];
pendientes.forEach( (pendientes) => console.log(pendientes))

const pendientes2 = ['Tarea', 'Comer', 'Proyectos', 'Estudiar JavaScript'];
