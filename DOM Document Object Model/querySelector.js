//Tiene una sintaxis similar a la de los selectores de CSS

//Retornara al menos un elemento que coincida , pero si no existe no retorna nada

const logo = document.querySelector('.logo');
console.log(logo);

const header = document.querySelector('.header');
console.log(header);
