/*

DEVUELVE EL NUMERO MAYOR DE CERO O MAS NUMEROS DADOS

  Math.max(1, 3, 2); // 3
  Math.max(-1, -3, -2); // -1
  
  const array1 = [1, 3, 2];
  Math.max(...array1); // 3

*/

function numeroMayorACero(){
  return Math.max(1, 473, 1838128, 122312, 32322);
}